{
  "name": "reduxdb",
  "version": "0.2.0",
  "main": "index.js",
  "types": "index.d.ts",
  "devDependencies": {
    "typescript": "^1.8.7"
  },
  "dependencies": {
    "es6-map": "^0.1.3",
    "object-assign": "^4.0.1",
    "object-path": "^0.9.2"
  },
  "peerDependencies": {
    "redux": "^3.3.1"
  },
  "scripts": {
    "build": "tsc --removeComments --out index.js src/index.ts",
    "watch": "tsc --removeComments --out index.js -w src/index.ts",
    "test": "tsc test/test_*.ts && ls test/test_*.js |xargs -I {} node {}"
  }
}
