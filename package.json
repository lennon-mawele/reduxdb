{
  "name": "reduxdb",
  "version": "2.1.3",
  "main": "index.js",
  "types": "reduxdb.d.ts",
  "scripts": {
    "watch": "tsc -d --outFile index.js -w js/index.ts",
    "build": "tsc -d --outFile index.js js/index.ts",
    "test": "npm run build && tsc test/test_*.ts && ls test/test_*.js |xargs -I {} node {}"
  },
  "repository": "github:wizawu/reduxdb",
  "dependencies": {
    "object-assign": "^4.0.1",
    "object-path": "^0.9.2",
    "redux": "^3.6.0"
  }
}
